# Copy to .env to override. The default (used when this is unset) is
# http://localhost:8000, which is where the backend's documented uvicorn
# command serves from — so for local development you don't need a .env at all.
VITE_API_BASE_URL=http://localhost:8000
